# Claude 续修与 520 封回放验证

2026-09-21。继续 Claude 已经修改的 provider.js / rules.js，补完未成功写入的配对流程。生产规则版本 verity-rules-1.2。

## 已完成

- 识别 BILL OF LADING INSTRUCTION(S) 为 SI；同一文件同时出现 SI、BL 标题仍标记歧义。原有缓存也执行新的原生文本恢复，不必重付提取费用。
- 配对从“必须有相同 Booking”扩展到原文中有证据的同类 Booking / Order / BL 引用。不同种类号码不直接比较；明确冲突、多个候选、无关联依据仍停止自动配对。
- 官方 XLSX 标题第二格是 so_number，来自 render.py:179。只在原件字节已注册的模板应用该约定，未对任意陌生表格猜测。支持内嵌 Booking 标签。
- 扩充港口词典、区分真实代码与普通单词；名称不同但代码相同仍报告差异。保留完整斜线地点列表。修正 Gdansk/Gdynia、Bula/Buatan 等代码混淆，参考 docs/port-reference.md。
- 公司字段处理表格分隔符、制表符标题；完整名称、法人后缀和实际代理关系仍保留。
- 重量从同一字段的单位、标题单位或已注册模板约定取单位；原始值、原文不改写。支持 Notify Party/Intermediate Consignee；TBA、TBD、下划线占位符保持缺值。
- 导出继续保留真实差异，不用缺资料覆盖已确定的差异；未定义的混合情况仍明确阻塞。

## 实测范围

使用保存的 520 封独立 AI 提取，调用实际生产 pipeline 重放原生恢复、配对、校验、比较、导出。网络被测试程序禁止；没有新 AI/OCR 调用或额外 API 费用。分类结果不变。6 条原来失败/不完整的模型结果保留，未伪造替代提取。

预测生成后才独立加载 ground truth 评分；生产规则不读取答案。由于用这批数据开发过规则，这是开发回归，不是独立留出集评估，不代表新文件准确率、模型速度或成本改善。

| 项目 | v2 | 本次 v5 |
|---|---:|---:|
| 全部邮件 | 520 | 520 |
| 自动完成 | 384 | 436 |
| 其中七字段核对完成 | 11 | 63 |
| 人工/异常队列 | 136 | 84 |
| 46 封真实差异检出 | 27 | 45 |
| 差异字段集合完全正确 | 17 | 45 |
| 误报邮件 | 3 | 3 |
| 漏检邮件 | 19 | 1 |
| 真实差异被误自动完成 | 0 | 0 |
| 可导出记录 | 433 | 494 |
| 导出阻塞 | 87 | 26 |

分类仍为 473/520。52/70 个上次判为工程问题的人工案例自动完成；剩余 18 个是分类待确认，本次没有重跑分类模型。其他队列：46 个真实差异业务案例（45 已检出，email_091 原提取失败），17 个源资料问题，3 个扫描问题。合计 84，不能把它们全部称为必须人工或全部称为软件错误。

剩余误报：email_512 的扫描识别错误；email_516、email_518 的代理关系源文本与官方判断范围冲突。漏检：email_091 原模型提取失败。下一轮应分别做受控扫描恢复、提取重试、分类复核和评分语义确认。

## 检查

62 项自动测试通过，生产构建通过。包括错单不配对、空编号不生成假关联、缓存原生恢复、港口名/代码冲突、完整斜线地点比较、原文单位恢复、真实差异与缺资料同时保留。

官方 score_cli 已对部分结果运行成功，仅作诊断：它会为缺失邮件默认补 GENERAL，因此成功退出及高分不代表可提交。完整导出仍有 26 条阻塞；submission-partial-NOT-FOR-SUBMISSION.json 不能正式提交。原始全量目录未改动。

回放目录：work/pipeline-regression-2026-09-21-v5（项目根目录的上一级）。包含逐封结果、summary.json、export-check.json 与 replay-metadata.json。
